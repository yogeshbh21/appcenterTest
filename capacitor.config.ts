import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.impirios.newapp',
  appName: 'angular.io-example',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
